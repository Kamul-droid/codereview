{% extends 'base.html.twig' %}

{% block title %}Mark{% endblock %}

{% block body %}
    <h1>Mark</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ mark.id }}</td>
            </tr>
            <tr>
                <th>Score</th>
                <td>{{ mark.score }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_mark_index') }}">back to list</a>

    <a href="{{ path('app_mark_edit', {'id': mark.id}) }}">edit</a>

    {{ include('mark/_delete_form.html.twig') }}
{% endblock %}
